:cpp: {basebackend@docbook:c++:cpp}

== Qt Components



[jle_temp_and_run]
+++++++++++++++++
run=[ ("echo '--------'",'ls.txt', 'wt'), 
      ("ls {docdir}/../../src/qt_components/src/ | grep .*\.h", 'ls.txt', 'at'),
      ("echo '--------'",'ls.txt', 'at')]
print ''
+++++++++++++++++

.src/qt_components/src
include::/tmp/adoc/ls.txt[]



=== mtkContainerWidget

* All widget in terminal will be inside this one
* It will give next functionalities
** Moving
** Resizing
** Request persistence
** Recover from persistence

.example pro
[source,{cpp}]
-----
include::../../examples/qt/ex_cont_widget/src/ex_cont_widget.pro[]
-----

.example code
[source,{cpp}]
-----
include::../../examples/qt/ex_cont_widget/src/mainwindow.cpp[]
-----
<1> Instanciating the contained widgets
<2> Preparing layout for contained widgets
<3> This call will raise the movement title and the small resize square

=== QOrderBook

* Connected to order book component with a hook


=== QEditOrder

* It lets user to fill data for new orders or modifications

