:cpp: {basebackend@docbook:c++:cpp}

=== Components -> Prices



==== prices_manager
* It will manage all publishing information related to a product (prices, status, etc...)
* It will request for load
* It will mantain the last received value
* It will signal updates
+
[source,{cpp}]
----
include::../../components/prices/cli/price_manager.h[]
----
+
* It works internally with factory, you just have to create an instance of this class
