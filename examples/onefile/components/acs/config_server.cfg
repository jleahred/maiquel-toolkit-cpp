ADMIN
{
    role:server
    priority:normal   #very_low, low, normal, high, critical.    when role is not client
    server_name:ex_acs_server_synchr

    URLS
    {
        admin:amqp:tcp:127.0.0.1:5672
        client:amqp:tcp:127.0.0.1:5672
        server:amqp:tcp:127.0.0.1:5672
    }
}

MISC
{
    data:data/acs.data
}
  
