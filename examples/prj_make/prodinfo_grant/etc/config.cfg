ADMIN
{
    role:server
    priority:low   #very_low, low, normal, high, critical.    when role is not client
    server_name:cGEN_PRODINF_GRANT

    URLS
    {
        admin:amqp:tcp:127.0.0.1:5672
        client:amqp:tcp:127.0.0.1:5672
        server:amqp:tcp:127.0.0.1:5672
    }
}

MISC
{
    data:data/grants.yaml
}
 